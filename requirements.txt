# Core dependencies for Teacher1 project - Python 3.12+ compatible
numpy>=2.3.0
matplotlib>=3.10.0

# GUI dependencies
# tkinter is built-in with Python but may need: sudo apt-get install python3-tk

# Speech and audio dependencies (optional - may require system audio setup)
SpeechRecognition>=3.10.0
pyttsx3>=2.90
# pyaudio  # Often requires system audio libraries: sudo apt-get install portaudio19-dev python3-dev

# BlenderBot chatbot framework - HuggingFace transformers
transformers>=4.55.0
torch>=2.0.0

# WebSocket communication dependencies
websockets>=15.0

# Web interface dependencies
flask>=3.1.0
flask-cors>=6.0.0